---
phase: 04-design-components
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - app/page.tsx
  - components/Footer.tsx
  - components/Header.tsx
autonomous: true
requirements: [COMP-04]

must_haves:
  truths:
    - "Homepage uses design tokens (alpine, frost, snow, granite) with zero dark: or legacy gray/cyan classes"
    - "Homepage stats section uses stratum depth shadows and frost glow on stat cards"
    - "Footer uses design tokens with stratified depth effect on the footer container"
    - "Header.tsx uses design tokens with frost glow active states (consistent with GlassmorphismNav treatment)"
    - "CTA section on homepage uses frost gradient instead of cyan-to-blue gradient"
    - "About section on homepage uses alpine backgrounds instead of white/gray"
    - "Skills pills use frost token colors instead of cyan/blue gradients"
  artifacts:
    - path: "app/page.tsx"
      provides: "Token-migrated homepage with geological depth shadows and frost glow accents"
      min_lines: 80
    - path: "components/Footer.tsx"
      provides: "Token-migrated footer with stratified depth effect and frost-themed social links"
      min_lines: 60
    - path: "components/Header.tsx"
      provides: "Token-migrated Header component with frost glow active states"
      min_lines: 60
  key_links:
    - from: "app/page.tsx"
      to: "app/globals.css"
      via: "uses stratum-1 and glow-frost CSS classes on stat cards"
      pattern: "stratum-1|glow-frost"
    - from: "app/page.tsx"
      to: "lib/animations.ts"
      via: "uses EASING for scroll-triggered entrance animations"
      pattern: "import.*EASING"
    - from: "components/Footer.tsx"
      to: "app/globals.css"
      via: "uses stratum-3 for deep geological depth on footer"
      pattern: "stratum-3"
---

<objective>
Migrate the homepage, footer, and Header component to design tokens with stratified depth effects and frost glow accents. This completes the visual identity across all primary page surfaces.

Purpose: After Plans 02-04 restyle the individual components (nav, cards, buttons), the pages that host them still use legacy gray/cyan/white colors. This plan completes the token migration for the three most visible surfaces: homepage (page.tsx), footer, and the secondary Header component used on sub-pages. Applying stratum shadows and frost glow to these surfaces completes the COMP-04 geological depth and glow accent system.

Output: Token-migrated page.tsx, Footer.tsx, Header.tsx with geological depth and frost glow styling
</objective>

<execution_context>
@C:/Users/aster/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-components/04-RESEARCH.md
@.planning/phases/04-design-components/04-CONTEXT.md
@.planning/phases/04-design-components/04-01-SUMMARY.md
@.planning/phases/04-design-components/04-02-SUMMARY.md

@app/page.tsx
@components/Footer.tsx
@components/Header.tsx
@lib/animations.ts
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate homepage to design tokens with geological depth and glow</name>
  <files>app/page.tsx</files>
  <action>
Migrate `app/page.tsx` to design tokens. This file has heavy legacy styling (10 dark: occurrences, 9 gray/cyan legacy classes).

**Token migration map** (apply systematically throughout the file):

Page background container (#main-content):
- `bg-gradient-to-b from-white to-gray-50 dark:from-gray-900 dark:to-gray-800` -> `bg-alpine-900`

Stats section cards:
- `bg-white dark:bg-gray-800` -> `bg-alpine-800`
- `border-2 border-gray-200 dark:border-gray-700` -> `border border-alpine-700`
- `hover:border-cyan-500 dark:hover:border-cyan-500` -> `hover:border-frost-500`
- `text-cyan-600 dark:text-cyan-400` -> `text-frost-400`
- `text-gray-600 dark:text-gray-400` -> `text-granite-400`
- Add `stratum-1 glow-frost` classes to each stat card for geological depth + frost glow accent
- Import `EASING, DURATION` from '@/lib/animations'. Update entrance animations: `transition={{ delay: index * 0.08, duration: DURATION.enter, ease: EASING.crystallize }}`

"Explore My Work" heading:
- `text-gray-900 dark:text-white` -> `text-snow-50`

Photography featured card:
- Keep the image overlay gradient as-is (dark overlay for text legibility is fine)
- `text-cyan-400` -> `text-frost-400`
- `text-gray-200` -> `text-snow-200`

Projects featured card:
- `bg-gradient-to-br from-purple-600 via-purple-700 to-pink-600` -> `bg-gradient-to-br from-frost-600 via-alpine-700 to-frost-500` (replace purple/pink with frost/alpine to match the crystalline theme)
- `text-purple-100` -> `text-snow-200`
- `text-pink-300` -> `text-frost-400`

About section:
- `bg-white dark:bg-gray-800` -> `bg-alpine-800`
- `border-2 border-gray-200 dark:border-gray-700` -> `border border-alpine-700`
- `bg-gradient-to-r from-cyan-600 to-blue-600 bg-clip-text text-transparent` -> `text-frost-400` (remove clip-text gradient, use frost color directly, the h2 base style from @layer base already provides the heading font)
- `text-gray-700 dark:text-gray-300` -> `text-snow-200`
- `border-gray-200 dark:border-gray-700` -> `border-alpine-700`
- `text-gray-900 dark:text-white` -> `text-snow-50`
- Skills pills: `bg-gradient-to-r from-cyan-50 to-blue-50 dark:from-cyan-900/30 dark:to-blue-900/30 text-cyan-700 dark:text-cyan-300 border-cyan-200 dark:border-cyan-800` -> `bg-frost-500/10 text-frost-400 border border-frost-600/30`
- Add `stratum-2` to the about container for geological depth

CTA section:
- `bg-gradient-to-r from-cyan-600 to-blue-600` -> `bg-gradient-to-r from-frost-600 to-frost-500`
- `text-cyan-100` -> `text-snow-200`
- `bg-white text-cyan-600` -> `bg-snow-50 text-alpine-950`
- `hover:bg-gray-100` -> `hover:bg-snow-100`
- `bg-white/10 border-white/30 text-white hover:bg-white/20` -> `bg-alpine-800/30 border-frost-500/30 text-snow-50 hover:bg-alpine-700/40`
- Add `stratum-2` class to the CTA container

Remove ALL `dark:` prefixes throughout the file.

Import EASING and DURATION from '@/lib/animations' and use geological easing on the entrance animations for stats and sections (replace default `duration: 0.5` with `duration: DURATION.enter, ease: EASING.geological`).
  </action>
  <verify>
Run `npx tsc --noEmit`. Run `npm run build`. Grep `dark:` in `app/page.tsx` -- should find zero. Grep `bg-white|text-gray-|from-cyan-600|from-purple-600` -- should find zero. Verify `stratum-1` and `glow-frost` appear on stat cards. Verify `EASING` is imported.
  </verify>
  <done>
Homepage uses alpine/frost/snow/granite tokens exclusively. Stats cards have stratum-1 depth + glow-frost accent. About section has stratum-2 geological depth. CTA section uses frost gradient. Skills pills use frost tokens. All entrance animations use geological easing. Zero dark: or legacy color classes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Footer and Header to design tokens with geological depth</name>
  <files>components/Footer.tsx, components/Header.tsx</files>
  <action>
**Migrate `components/Footer.tsx`:**

Token migration map:
- `bg-gradient-to-b from-gray-900 to-black` -> `bg-alpine-950`
- `text-white` -> `text-snow-50` (where used as primary text)
- `bg-gradient-to-r from-cyan-400 to-blue-400 bg-clip-text text-transparent` -> `text-frost-400` (brand heading, remove clip-text)
- `text-gray-400` -> `text-granite-400`
- `bg-gray-800` -> `bg-alpine-800`
- `hover:from-cyan-500 hover:to-blue-500` -> `hover:bg-frost-500`
- `hover:shadow-cyan-500/50` -> `hover:shadow-frost-500/50`
- `text-cyan-400` -> `text-frost-400`
- `bg-cyan-500` -> `bg-frost-500`
- `hover:text-white` -> `hover:text-snow-50`
- `border-gray-800` -> `border-alpine-700`
- `text-gray-500` -> `text-granite-500`
- `text-red-500` -> `text-ember-500` (heart emoji color)

Add `stratum-3` class to the footer element for the deepest geological depth layer.

Add `glow-frost` class to social link icons on hover state.

Import `EASING, DURATION` from '@/lib/animations'. Replace entrance animation transitions with geological easing.

Remove ALL `dark:` prefixes (there shouldn't be any in Footer, but verify).

**Migrate `components/Header.tsx`:**

This component is used on sub-pages (projects, photos, roadmap, contact) as the non-glassmorphism nav. Apply consistent styling with the redesigned GlassmorphismNav.

Token migration map:
- `bg-white/80 dark:bg-gray-900/80` -> `bg-alpine-900/80`
- `bg-white/60 dark:bg-gray-900/60` -> `bg-alpine-900/60`
- Logo gradient: `from-cyan-500 to-blue-600` -> `from-frost-500 to-frost-600`
- Logo text: `from-cyan-600 to-blue-600 dark:from-cyan-400 dark:to-blue-400 bg-clip-text text-transparent` -> `text-frost-400` (remove clip-text, use direct frost color)
- `shadow-cyan-500/50` -> `shadow-frost-500/50`
- Active nav: `bg-gradient-to-r from-cyan-500 to-blue-600 text-white shadow-cyan-500/30` -> `bg-frost-500/20 text-snow-50 glow-frost`
- Active indicator: `bg-gradient-to-r from-cyan-500 to-blue-600` -> `bg-frost-500/30`
- Default nav: `text-gray-700 dark:text-gray-300` -> `text-snow-200`
- Hover nav: `hover:bg-gray-100 dark:hover:bg-gray-800` -> `hover:bg-alpine-700/50`
- Mobile button: `text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800` -> `text-snow-200 hover:bg-alpine-700/50`
- Mobile active: `from-cyan-500 to-blue-600 text-white` -> `bg-frost-500/20 text-snow-50`
- Mobile default: `text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800` -> `text-snow-200 hover:bg-alpine-700/50`
- Bottom gradient border: `via-cyan-500` -> `via-frost-500`

Import `EASING, DURATION` from '@/lib/animations'. Replace existing transition springs/durations with geological easing where appropriate (nav link animations, layout transitions).

Update the `layoutId="activeTab"` transition to use `ease: EASING.geological`.

Remove ALL `dark:` prefixes.
  </action>
  <verify>
Run `npx tsc --noEmit`. Run `npm run build`. Grep `dark:` in Footer.tsx and Header.tsx -- should find zero each. Grep `bg-gray-|text-gray-|from-cyan-` in both files -- should find zero. Verify `stratum-3` appears in Footer. Verify `glow-frost` appears in Header active state. Verify EASING is imported in both files.
  </verify>
  <done>
Footer uses alpine-950 background with stratum-3 geological depth, frost-themed social links, granite caption text, ember heart color. Header uses alpine-900 backgrounds with frost glow active states matching GlassmorphismNav treatment. Both files use geological easing for animations. Zero dark: or legacy color classes in either file.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` passes
3. app/page.tsx has zero `dark:` occurrences, uses stratum/glow classes
4. components/Footer.tsx has zero `dark:` or legacy color occurrences, has stratum-3
5. components/Header.tsx has zero `dark:` or legacy color occurrences, has glow-frost active state
6. All three files import EASING from lib/animations
7. Stat cards on homepage have stratum-1 + glow-frost
8. CTA section uses frost gradient (not cyan-to-blue)
</verification>

<success_criteria>
- Homepage, Footer, and Header exclusively use design tokens
- Geological depth shadows visible on stat cards (stratum-1), about section (stratum-2), CTA (stratum-2), footer (stratum-3)
- Frost glow accents on stat cards and nav active states
- Header styling consistent with GlassmorphismNav (frost glow, alpine backgrounds)
- All animations use geological easing
- Zero legacy gray/cyan/white/dark: classes in any modified file
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-components/04-05-SUMMARY.md`
</output>
