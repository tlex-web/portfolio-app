---
phase: 06-integration-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - middleware.ts
  - proxy.ts
  - public/service-worker.js
  - package.json
  - app/layout.tsx
  - next.config.ts
autonomous: true
requirements:
  - SEC-01
  - REL-01
  - REL-02
must_haves:
  truths:
    - "Next.js executes CSP middleware on page requests -- CSP header appears in responses"
    - "Upstash rate-limiting packages are installed and importable at runtime"
    - "Running npm run build replaces __BUILD_HASH__ placeholder in service worker with actual build ID"
  artifacts:
    - path: "middleware.ts"
      provides: "CSP middleware entry point"
      contains: "export function middleware"
    - path: "package.json"
      provides: "Upstash dependencies and postbuild wiring"
      contains: "@upstash/ratelimit"
    - path: "public/service-worker.js"
      provides: "Build-hash placeholder for injection"
      contains: "__BUILD_HASH__"
  key_links:
    - from: "middleware.ts"
      to: "Next.js runtime"
      via: "File convention (middleware.ts at project root)"
      pattern: "export function middleware"
    - from: "package.json postbuild"
      to: "scripts/inject-build-hash.mjs"
      via: "npm lifecycle hook"
      pattern: "inject-build-hash"
    - from: "scripts/inject-build-hash.mjs"
      to: "public/service-worker.js"
      via: "__BUILD_HASH__ placeholder replacement"
      pattern: "__BUILD_HASH__"
---

<objective>
Fix three cross-phase wiring breaks found by the v1.0 milestone audit: (1) rename proxy.ts to middleware.ts so Next.js executes CSP middleware, (2) install missing @upstash/ratelimit and @upstash/redis packages, (3) wire inject-build-hash.mjs into postbuild and restore the __BUILD_HASH__ placeholder in service-worker.js.

Purpose: All Phase 2 security and reliability code already exists but never runs due to naming, dependency, and script wiring errors.
Output: Working middleware, installed packages, and build-hash injection pipeline.
</objective>

<execution_context>
@C:/Users/tim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-integration-wiring-fixes/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename proxy.ts to middleware.ts and update references</name>
  <files>middleware.ts, proxy.ts, app/layout.tsx, next.config.ts</files>
  <action>
1. Rename `proxy.ts` to `middleware.ts` using `git mv proxy.ts middleware.ts`.
2. In `middleware.ts`, rename the exported function from `proxy` to `middleware` (line 3: `export function proxy(` becomes `export function middleware(`).
3. In `middleware.ts`, remove `raw.githack.com` from the connect-src directive (line 12), keeping only `https://raw.githubusercontent.com`. This aligns with Phase 2 decision [02-06] which corrected the CDN domain.
4. In `app/layout.tsx` line 32, update the comment from "proxy.ts" to "middleware.ts": `// Read nonce from middleware.ts x-nonce header -- forces dynamic rendering`
5. In `next.config.ts` line 41, update the comment from "proxy.ts" to "middleware.ts": `// Security headers (CSP moved to middleware.ts for nonce-based approach)`

Do NOT modify any CSP directive values beyond the connect-src raw.githack.com removal. The CSP logic is correct as-is.
  </action>
  <verify>
    <automated>cd C:/Users/tim/MyDocuments/Programming/portfolio_app && test -f middleware.ts && ! test -f proxy.ts && grep -c "export function middleware" middleware.ts | grep -q 1 && ! grep -r "proxy\.ts" app/layout.tsx next.config.ts && ! grep "raw.githack.com" middleware.ts && echo "PASS"</automated>
  </verify>
  <done>middleware.ts exists with exported `middleware` function, proxy.ts is gone, no stale "proxy.ts" references in layout or config comments, connect-src only has raw.githubusercontent.com</done>
</task>

<task type="auto">
  <name>Task 2: Install Upstash packages, wire postbuild script, restore SW placeholder</name>
  <files>package.json, public/service-worker.js</files>
  <action>
1. Install Upstash packages as production dependencies: `npm install @upstash/ratelimit @upstash/redis`
2. In `package.json`, update the `postbuild` script from:
   `"postbuild": "node scripts/verify-contrast.mjs"`
   to:
   `"postbuild": "node scripts/inject-build-hash.mjs && node scripts/verify-contrast.mjs"`
   Order matters: inject-build-hash reads `.next/BUILD_ID` (must exist from build), then verify-contrast runs after.
3. In `public/service-worker.js` line 4, replace the stale hardcoded hash with the placeholder:
   Change: `const BUILD_HASH = 'T5Omvo7bhPVP9HRwZj9-X';`
   To: `const BUILD_HASH = '__BUILD_HASH__';`

Do NOT modify any other service worker logic. Do NOT change the inject-build-hash.mjs script itself.
  </action>
  <verify>
    <automated>cd C:/Users/tim/MyDocuments/Programming/portfolio_app && npm ls @upstash/ratelimit @upstash/redis && grep "inject-build-hash" package.json && grep "__BUILD_HASH__" public/service-worker.js && echo "PASS"</automated>
  </verify>
  <done>@upstash/ratelimit and @upstash/redis appear in package.json dependencies and are installed in node_modules. postbuild script runs inject-build-hash.mjs before verify-contrast.mjs. service-worker.js has __BUILD_HASH__ placeholder on line 4.</done>
</task>

</tasks>

<verification>
All three wiring breaks are resolved:
1. `middleware.ts` exists at project root with `export function middleware` -- Next.js will execute it
2. `npm ls @upstash/ratelimit @upstash/redis` exits 0 -- packages available at runtime
3. `grep "inject-build-hash" package.json` matches in postbuild -- build-hash injection runs after each build
4. `grep "__BUILD_HASH__" public/service-worker.js` matches -- placeholder ready for injection
5. No remaining references to "proxy.ts" in source code (outside .planning/ and node_modules)
</verification>

<success_criteria>
- middleware.ts exists, proxy.ts does not exist
- CSP middleware function exported as `middleware` (not `proxy`)
- @upstash/ratelimit and @upstash/redis in package.json dependencies
- postbuild script chains inject-build-hash.mjs before verify-contrast.mjs
- service-worker.js contains `__BUILD_HASH__` placeholder (not a hardcoded hash)
- No stale "proxy.ts" comment references in app/layout.tsx or next.config.ts
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-wiring-fixes/06-01-SUMMARY.md`
</output>
