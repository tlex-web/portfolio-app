---
phase: 02-security-reliability-hardening
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: [proxy.ts]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "CSP style-src allows React inline style attributes without nonce interference"
    - "CSP connect-src allows Three.js HDRI loading from the correct drei CDN domain"
  artifacts:
    - path: "proxy.ts"
      provides: "Corrected CSP directives"
      contains: "style-src 'self' 'unsafe-inline'"
  key_links:
    - from: "proxy.ts"
      to: "React inline styles"
      via: "style-src directive"
      pattern: "style-src 'self' 'unsafe-inline'"
    - from: "proxy.ts"
      to: "drei CDN assets"
      via: "connect-src directive"
      pattern: "connect-src 'self' https://raw\\.githubusercontent\\.com"
---

<objective>
Fix two CSP directive bugs in proxy.ts that were introduced during prior gap closure rounds.

Purpose: Unblock phase 2 UAT by correcting style-src (nonce causes unsafe-inline to be ignored per CSP Level 2 spec) and connect-src (wrong CDN domain for drei assets).
Output: Corrected proxy.ts with working CSP directives.
</objective>

<execution_context>
@C:/Users/aster/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@proxy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CSP style-src and connect-src directives</name>
  <files>proxy.ts</files>
  <action>
Two changes on two lines in the cspHeader template literal in proxy.ts:

1. Line 9 — Remove 'nonce-${nonce}' from style-src. Change:
   style-src 'self' 'nonce-${nonce}' 'unsafe-inline';
   to:
   style-src 'self' 'unsafe-inline';
   Reason: Per CSP Level 2 spec, 'unsafe-inline' is ignored when a nonce is present. React uses inline style attributes which need 'unsafe-inline' to work.

2. Line 12 — Change the wrong CDN domain in connect-src. Change:
   connect-src 'self' https://raw.githack.com;
   to:
   connect-src 'self' https://raw.githubusercontent.com;
   Reason: drei assets are hosted on raw.githubusercontent.com, not raw.githack.com.

No other changes. Do not touch script-src (it correctly uses the nonce). Do not modify any other part of the file.
  </action>
  <verify>
After editing, confirm:
- Run: grep "style-src" proxy.ts — must show "style-src 'self' 'unsafe-inline'" with NO nonce
- Run: grep "connect-src" proxy.ts — must show "connect-src 'self' https://raw.githubusercontent.com"
- Run: npx tsc --noEmit proxy.ts or npm run typecheck — must pass (no syntax errors introduced)
  </verify>
  <done>
proxy.ts style-src contains only 'self' 'unsafe-inline' (no nonce), and connect-src points to https://raw.githubusercontent.com. File compiles without errors.
  </done>
</task>

</tasks>

<verification>
- grep proxy.ts for style-src confirms no nonce present
- grep proxy.ts for connect-src confirms raw.githubusercontent.com domain
- npm run build succeeds (CSP string is valid)
</verification>

<success_criteria>
- style-src directive is "'self' 'unsafe-inline'" only — no nonce token
- connect-src directive allowlists https://raw.githubusercontent.com — not raw.githack.com
- Application builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-reliability-hardening/02-06-SUMMARY.md`
</output>
