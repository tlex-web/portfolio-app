---
phase: 03-design-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components/ProjectDetailClient.tsx
  - data/projects.ts
autonomous: true
requirements:
  - DSGN-03
  - DSGN-02
gap_closure: true
must_haves:
  truths:
    - "npm run verify-contrast executes successfully and exits 0"
    - "npm run prebuild and npm run postbuild script entries exist in package.json"
    - "About This Project section renders styled, readable content against dark background"
    - "Project longDescription contains concise portfolio-appropriate content, not raw README dumps"
  artifacts:
    - path: "package.json"
      provides: "verify-contrast, prebuild, postbuild script entries"
      contains: "verify-contrast"
    - path: "components/ProjectDetailClient.tsx"
      provides: "Styled project description section with design-token-aware classes"
    - path: "data/projects.ts"
      provides: "Concise, personalised longDescription content for each project"
  key_links:
    - from: "package.json"
      to: "scripts/verify-contrast.mjs"
      via: "npm run verify-contrast script entry"
      pattern: "verify-contrast.*verify-contrast.mjs"
---

<objective>
Close two UAT gaps from Phase 3 Design Foundation:

1. Restore missing npm script entries (verify-contrast, prebuild, postbuild) that were accidentally removed during a dependency bump commit
2. Fix the "About This Project" section: replace raw README markdown dumps with concise, personalised project descriptions and apply proper styling using design tokens instead of broken prose classes

Purpose: Complete Phase 3 UAT compliance -- all 5 tests must pass
Output: Fixed package.json scripts, restyled ProjectDetailClient.tsx, rewritten project descriptions
</objective>

<execution_context>
@C:/Users/tim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-foundation/03-02-SUMMARY.md

<interfaces>
<!-- Design tokens available from globals.css @theme (established in 03-01) -->
<!-- Background layers: bg-alpine-950, bg-alpine-900, bg-alpine-800, bg-alpine-700 -->
<!-- Text colors: text-snow-50 (brightest), text-snow-100 (body), text-snow-200 (secondary), text-granite-400 (captions) -->
<!-- Accent colors: text-frost-500 (interactive cyan), text-ember-500 (emphasis amber) -->
<!-- Font tokens: font-heading (JetBrains Mono), font-body (Inter) -->

From data/types.ts:
```typescript
export interface Project {
  slug: string;
  name: string;
  tagline: string;
  shortDescription: string;
  longDescription: string; // Full description (plain text or Markdown)
  techStack: string[];
  links: { github?: string; demo?: string; docs?: string; };
  status: 'active' | 'beta' | 'complete' | 'archived';
  featured: boolean;
  features?: string[];
  highlights?: Record<string, HighlightValue>;
  demoCommands?: DemoCommand[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore missing npm script entries in package.json</name>
  <files>package.json</files>
  <action>
Add the following three script entries back to the "scripts" object in package.json (they were removed in commit ad73172 during an eslint dependency bump):

1. `"verify-contrast": "node scripts/verify-contrast.mjs"` -- runs the WCAG AA contrast verification script
2. `"prebuild": "node scripts/optimize-images.mjs"` -- runs image optimization before build
3. `"postbuild": "node scripts/verify-contrast.mjs"` -- runs contrast verification after build

Add them after the existing "optimize-images" entry to keep related scripts grouped together. Do NOT modify any other scripts or dependencies.

Verify the script actually runs: execute `npm run verify-contrast` and confirm it exits 0 with all 24 combinations passing.
  </action>
  <verify>
    <automated>cd "C:/Users/tim/MyDocuments/Programming/portfolio_app" && npm run verify-contrast</automated>
  </verify>
  <done>npm run verify-contrast exits 0 with all 24 PASS results. prebuild and postbuild entries present in package.json scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw README descriptions and fix About This Project styling</name>
  <files>data/projects.ts, components/ProjectDetailClient.tsx</files>
  <action>
**Part A: Rewrite longDescription in data/projects.ts**

Replace the raw README markdown dumps with concise, portfolio-appropriate descriptions (3-5 short paragraphs each, plain text, no markdown headers or emoji). Write them as a developer presenting their work to a potential employer or collaborator -- confident, specific, personal.

For `clix-cli` project, write a description that covers:
- What CLI_X is and why it was built (1 paragraph, personal motivation)
- The core technical approach: 4-stage intelligence pipeline, multi-provider LLM support, Rust for performance (1 paragraph)
- The safety system: ML-inspired risk scoring as a differentiator (1 paragraph)
- Key metrics: 90ms startup, 850+ tests, 11 safety rules (weave into prose, not a list)

For `clix-website` project, write a description that covers:
- What the platform does and its relationship to CLI_X (1 paragraph)
- Technical architecture: Next.js 14 with Stripe, NextAuth, Prisma (1 paragraph)
- Key capabilities: interactive demo playground, license management, multi-platform distribution (1 paragraph)

Keep each description under 600 characters. No markdown formatting -- just clean paragraphs separated by \n\n.

**Part B: Restyle ProjectDetailClient.tsx**

Replace the broken `prose prose-lg dark:prose-invert` classes with design-token-aware styling. The section renders plain text now (not markdown) so ReactMarkdown is no longer needed for the description.

1. Remove the dynamic import of ReactMarkdown entirely
2. Replace the "About This Project" section with a styled `<div>` that splits `project.longDescription` on double-newlines and renders each paragraph as a `<p>` element
3. Use these design token classes:
   - Section heading: `text-2xl font-heading font-semibold text-snow-50 mb-6`
   - Container: `bg-alpine-800 rounded-xl p-8 border border-alpine-600`
   - Paragraphs: `text-snow-100 leading-relaxed mb-4 last:mb-0`
4. Rename the section heading from "About This Project" to "Overview"
5. Keep the existing Hologram Terminal section above unchanged
6. Keep the existing loading placeholder for HologramTerminal/HologramTerminalDemo unchanged -- only change the description section styling
  </action>
  <verify>
    <automated>cd "C:/Users/tim/MyDocuments/Programming/portfolio_app" && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ProjectDetailClient.tsx renders paragraphs with design token classes (bg-alpine-800, text-snow-100), no prose classes remain. Both project longDescriptions are concise portfolio-style text under 600 chars. Build succeeds with no type errors.</done>
</task>

</tasks>

<verification>
1. `npm run verify-contrast` exits 0 with all 24 combinations passing
2. `grep -c "verify-contrast\|prebuild\|postbuild" package.json` returns 3+ (script entries exist)
3. `grep -c "prose" components/ProjectDetailClient.tsx` returns 0 (no prose classes remain)
4. `grep -c "ReactMarkdown" components/ProjectDetailClient.tsx` returns 0 (ReactMarkdown removed)
5. Build passes: `npm run build` exits 0
</verification>

<success_criteria>
- npm run verify-contrast runs and passes all 24 combinations (UAT Test 5 fixed)
- About This Project section uses design tokens and renders concise readable text (UAT Test 4 fixed)
- No raw README dumps in longDescription fields
- Build and typecheck pass without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-foundation/03-03-SUMMARY.md`
</output>
